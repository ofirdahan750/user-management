/**
 * Production environment configuration.
 * Used when running `ng build --configuration=production` or `ng build`.
 */
export const environment = {
  production: true,
  apiUrl: 'https://cheerful-rois-organizationnametwo-fc78dba0.koyeb.app/api',
};
